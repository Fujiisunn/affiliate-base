// ===========================================================================
//  広告コードはここに貼るだけ
// ===========================================================================
//  忍者AdMax / Google AdSense などの広告タグを、出したい位置の文字列に貼る。
//  - 空文字 '' のままなら、その枠は「何も表示されない」(枠ごと消える)。
//  - ここに貼ったコードは、全記事の該当位置に自動で反映される。
//  - <script> を含むタグでもOK(実行されるように仕込んである)。
//
//  ※ Google AdSense を使う場合は、ローダー(adsbygoogle.js の <script>)を
//     src/layouts/BaseLayout.astro の <head> 内の目印コメント箇所に1回だけ貼り、
//     ここには各広告ユニットの <ins class="adsbygoogle">...</ins> 部分を貼る。
// ===========================================================================

export const AD_SLOTS = {
  // 記事冒頭(ファーストビュー)— 最も見られる・単価が高い
  top: ``,

  // 本文中 — .mdx 記事内に <AdSlot position="incontent" /> を手動で置いた箇所に出る
  incontent: ``,

  // 記事末尾 — 読み終わった人に当たる
  bottom: ``,

  // サイドバー(PC表示時)
  sidebar: ``,
} as const;

export type AdPosition = keyof typeof AD_SLOTS;
